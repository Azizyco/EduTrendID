############################################################
# .env.example - EduTrendID (Supabase Mode)
############################################################
# Salin file ini menjadi .env lalu ganti placeholder.
# Jangan commit file .env yang berisi password/key sensitif.
############################################################

PORT=3000
NODE_ENV=development
SKIP_DB_CHECK=false
USE_PLAIN_PASSWORD=false  # true = SIMPAN PASSWORD TANPA HASH (TIDAK AMAN, hanya untuk demo)

# Supabase PostgreSQL Connection (GANTI PASSWORD)
SUPABASE_DB_URL=postgres://postgres:REPLACE_WITH_REAL_PASSWORD@db.iwgbpotbpdxdtqtorvfm.supabase.co:5432/postgres

# Opsional granular (boleh dikosongkan jika SUPABASE_DB_URL sudah benar)
SUPABASE_DB_HOST=db.iwgbpotbpdxdtqtorvfm.supabase.co
SUPABASE_DB_PORT=5432
SUPABASE_DB_USER=postgres
SUPABASE_DB_PASSWORD=REPLACE_WITH_REAL_PASSWORD
SUPABASE_DB_NAME=postgres

# Supabase API
SUPABASE_URL=https://iwgbpotbpdxdtqtorvfm.supabase.co
SUPABASE_ANON_KEY=REPLACE_WITH_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY= # JANGAN isi di contoh (hanya di server private)

# JWT Secret untuk token aplikasi custom (BUAT RANDOM / panjang)
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_SECRET_STRING

# Catatan:
# 1. USE_PLAIN_PASSWORD=false artinya password di-hash bcrypt.
# 2. Jika USE_PLAIN_PASSWORD=true: login akan membandingkan plain text (risiko keamanan).
# 3. Untuk produksi SELALU gunakan bcrypt (false) dan rotate JWT_SECRET secara berkala.
# 4. Jika koneksi database gagal, cek SUPABASE_DB_URL & password.
# 5. SKIP_DB_CHECK=true hanya untuk debug server start tanpa DB.
############################################################
